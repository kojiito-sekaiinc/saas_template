Small status/label pill, 6px radius. `tone` selects the semantic color (neutral/primary/success/warning/danger); `dot` adds a leading indicator dot. Always pair color with text — never color alone.

```jsx
<Badge tone="success" dot>実現済み</Badge>
<Badge tone="primary">進行中</Badge>
<Badge>下書き</Badge>
```
