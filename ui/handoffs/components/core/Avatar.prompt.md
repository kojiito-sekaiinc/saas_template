Circular avatar — renders `src` as a cover image, or falls back to initials from `name` on a subtle gray fill. Sizes `sm`/`md`/`lg` or a number.

```jsx
<Avatar name="Koji Ito" />
<Avatar src="/assets/me.jpg" name="Koji Ito" size="lg" />
```
