Content grouping container — white surface, subtle border, 8px radius, no shadow by default. Optional `title`/`subtitle` header (or custom `header`/`footer` nodes). Set `interactive` for a hover border on clickable cards.

```jsx
<Card title="システム状況">…</Card>
<Card interactive onClick={open} padding="var(--space-4)">…</Card>
```
