Thin 6px progress bar — blue fill on a subtle track. Use for overall dream completion (e.g. 37 / 100 realized). Optional `label` and `showValue` percentage.

```jsx
<ProgressBar value={37} max={100} label="実現した夢" showValue />
<ProgressBar value={80} tone="success" />
```
