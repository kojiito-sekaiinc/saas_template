[pytest]
DJANGO_SETTINGS_MODULE = config.settings_test
python_files = tests.py test_*.py *_tests.py
; 通常の pytest は apps/ 配下の unit/integration テストのみ実行する。
; tests/e2e はブラウザ（playwright install）が必要なため既定では収集しない。
; E2E の実行は明示的に: pytest tests/e2e
testpaths = apps
addopts = -v
